{
  "name": "al-docker4drupal",
  "version": "1.0.0",
  "description": "activelamp.com on Docker4Drupal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "(cd node_modules/reactivelamp && npm start -s)",
    "install-reactivelamp": "(cd node_modules/reactivelamp && npm install)"
  },
  "author": "ActiveLAMP",
  "license": "ISC",
  "dependencies": {
    "reactivelamp": "github:activelamp/reactivelamp"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "npm-run-all": "^4.0.2"
  }
}
